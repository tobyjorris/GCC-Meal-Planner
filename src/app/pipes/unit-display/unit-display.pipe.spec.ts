import { UnitDisplayPipe } from './unit-display.pipe';

describe('UnitDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
